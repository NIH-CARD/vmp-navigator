# The "Learn more" assistant dependency. This is now ALSO listed in requirements.txt so
# that Streamlit Community Cloud (which installs only requirements.txt) gets it; this file
# remains for documentation and minimal local installs. The base app runs fine without a
# key — the import is lazy and the feature degrades to curated text.
#   pip install -r requirements-ai.txt
#   export ANTHROPIC_API_KEY=sk-...        # presence of a key is the opt-in
anthropic>=0.40
