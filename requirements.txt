requests>=2.31
streamlit>=1.32
pyyaml>=6.0
# Needed for the optional "Learn more" assistant. Kept here (not only in
# requirements-ai.txt) because Streamlit Community Cloud installs ONLY this file;
# without it `import anthropic` fails on Cloud and every AI call silently falls back.
# The app still runs without a key — the import is lazy and degrades to curated text.
anthropic>=0.40
