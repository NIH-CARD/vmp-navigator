---
description: Start the Streamlit app
---
Start the app with `streamlit run app.py`. Remind the user the "Learn more" assistant is live only if `ANTHROPIC_API_KEY` is set in `.streamlit/secrets.toml`; otherwise the curated text shows. Never print or read the contents of `.streamlit/secrets.toml`.
