# Copy to .streamlit/secrets.toml and add your key to enable the live "Learn more" assistant.
# Never commit the real secrets.toml. Synthetic-data testing only.
ANTHROPIC_API_KEY = "sk-ant-..."

# Optional: override the model. Default is claude-opus-4-8 (Claude 4.8).
# For this grounded education use case, a cheaper/faster model is usually plenty:
#   VMP_AI_MODEL = "claude-haiku-4-5-20251001"   # or "claude-sonnet-4-6"
# VMP_AI_MODEL = "claude-opus-4-8"
